[tool.poetry]
name = "musicparser"
version = "0.1.0"
description = ""
authors = ["Luffious <segasidorov@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^5.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
